apiVersion: meta.pkg.crossplane.io/v1alpha1
kind: Function
metadata:
  name: function-eventdrivenservice
  annotations:
    meta.crossplane.io/maintainer: Platform Team
    meta.crossplane.io/source: github.com/arun4infra/zerotouch-platform
    meta.crossplane.io/license: Apache-2.0
    meta.crossplane.io/description: |
      Composition function for EventDrivenService that handles dynamic secret mounting
      and optional init container configuration.
    meta.crossplane.io/readme: |
      This function processes EventDrivenService claims and builds:
      - Dynamic env arrays from secretRefs with individual key mappings
      - Dynamic envFrom arrays from secretRefs with bulk mounting
      - Optional init containers with same secret access as main container
spec:
  package: ghcr.io/arun4infra/function-eventdrivenservice:v0.1.0
